import { FilmFavoFilterPipe } from './film-favo-filter.pipe';

describe('FilmFavoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmFavoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
